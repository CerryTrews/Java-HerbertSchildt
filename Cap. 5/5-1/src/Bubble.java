public class Bubble {
    // Base da classificação de bolha

        /*
        int a, b;

        for(a = 1; a < size; a++ ) {
            if(nums [b-1] > nums [b]) {
                t = nums[b-1];
                nums[b-1] = nums[b];
                nums[b] = t;
            }
        }
        */
}
